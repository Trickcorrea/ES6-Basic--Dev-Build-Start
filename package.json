{
  "name": "basico-es6",
  "version": "1.0.0",
  "description": "Simples e basico ambiente para desenvolvimento com ES6",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node src --exec \"babel-node src --presets env\"",
    "build": "rimraf dist && babel src -s -D -d dist --presets env",
    "start": "node dist"
  },
  "keywords": [],
  "author": "RocketSeat - modified by Patrick Corrêa ",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "rimraf": "^2.6.3"
  }
}
